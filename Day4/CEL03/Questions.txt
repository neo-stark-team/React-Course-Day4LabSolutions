Question:

Topic : Functional Component Exercise

Question : 
App component is the root component of the application.
Books component is the wrapper component to display book items.
BookItem component is the reusable component which will display a single book item in the UI.

App component is the parent component of the Books component and Books component is the parent component of the BookItem component.

Create a list of books in App Component and pass this list as “books” attribute to Books component to display the books in UI.
Create BookItem component accessing each book using props

